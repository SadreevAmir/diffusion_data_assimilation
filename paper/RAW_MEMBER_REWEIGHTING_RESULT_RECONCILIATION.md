# Raw-member reweighting result reconciliation

Status: PRE_RESULT_NO_TRUSTED_MODE

This is the outcome-agnostic, fail-closed publication handoff for
`NEXT_RAW_MEMBER_REWEIGHTING_CONTRACT.md`. It neither names nor implements a
trusted mode and is not scientific evidence. Reconciliation is permitted only
after the controller-visible review record passes
`validate_raw_member_reweighting_review.py` with `admission=GO` and the admitted
full run returns its exact `summary_only` compact evidence.

## Authoritative input and immutable identities

Before interpreting any metric, record and verify the literal reviewed mode,
experiment identifier, review-record SHA-256, runner SHA-256, contract SHA-256,
synthetic-result SHA-256, candidate identifier, completed case count and
ensemble size. Until then every value remains `PENDING` and no manuscript claim
changes.

The decision-bearing compact result must contain the full gate, raw and
candidate aggregates, paired date uncertainty, fixed four-case-block
sensitivity, fold and analog provenance, rank-bin probabilities, source-index
multiplicities, effective sample sizes, exact-copy and mask invariants, and
operational counts. Missing or contradictory evidence is an operational
failure; an aggregate metric may never substitute for a family Boolean.

## Mutually exclusive scientific branches

### Positive branch

This branch is legal only when proper-score, finite-ensemble reliability,
boundary, spatial/physical and operational families and `overall_eligible` are
all literally true. Report the predeclared fair-CRPS improvement, paired date
interval, ordinary-CRPS guard, absolute rank decision, every attainable
coverage displacement, boundary and spatial diagnostics, and identity
invariants. Limit the claim to leakage-safe probability-mass reassignment among
unchanged learned-joint scenarios on the development envelope.

### Negative branch

This branch is mandatory when any family or `overall_eligible` is false. Name
every failed family without compensation. Reliability failure rejects
probability-mass misallocation among existing scenarios as a sufficient
explanation; proper-score failure rejects useful reweighting; mean-field
spatial/physical failure shows damaging scenario-frequency changes; copy, mask
or operational failure invalidates execution. No smoothing, analog, feature,
fold, tie or sampling choice may be retuned.

## Atomic publication update map

After valid admission, update `PAPER_DRAFT.md`, `CLAIM_LEDGER.md`,
`PUBLICATION_READINESS.md`, `REPRODUCIBILITY.md` and this file in one worktree
change. All five surfaces must carry identical experiment, candidate, review
identity, completed counts, family Booleans and decision. A partial transition
is not publication evidence.

## Fail-closed consistency rules

- Pre-result status requires all immutable identities and results to remain
  `PENDING` and forbids any reconciled-result marker.
- A positive status requires every mandatory family and
  `overall_eligible=true`; a negative status requires at least one failed family
  and `overall_eligible=false`.
- The completed envelope must be exactly 40 cases by 10 members.
- The paired date interval is decision-bearing for the frozen fair-CRPS claim;
  the four-case-block interval remains sensitivity only.
- A negative result closes this exact probability-mass mechanism without
  retuning and does not authorize another independent evaluation.
