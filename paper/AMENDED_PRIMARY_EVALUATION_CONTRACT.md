# Amended primary evaluation contract

Status: DEPLOYED_AUDITED_PRIMARY_IN_FLIGHT

This contract supersedes every earlier scientific success decision. Completed
development gates remain useful mechanism evidence, but none of their recorded
`overall_eligible` values is a final primary success decision.

## Immutable contract identity

- primary evaluation contract:
  `f2225da7a05cab53b14604e45bed840a0ec559aed20856ae8ef2dd72d915b8f8`
- gate contract:
  `57e8dd1859c4ac9a144be68904450926a6098b08a3b58a35e3d7dcc6a5bb9185`

The trusted controller has deployed and audited both exact digests and admitted
the single signed primary chain. This worktree does not implement, substitute or
launch that runner. A digest mismatch, absent audit attestation or fallback to
an earlier gate fails closed. Admission and stage completion metadata do not
constitute a scientific outcome; only the complete compact primary result does.

## Amended scientific decision rules

1. A good randomized rank histogram is an absolute requirement. Relative
   improvement over raw is reportable but cannot compensate for failure of the
   absolute uniformity criterion fixed by the gate contract.
2. High-SIC behaviour is compared with truth at
   `q={0,.15,.90,.95,.99}`. These truth-referenced diagnostics replace the old
   rule requiring near-one mass to be no worse than raw.
3. `>=.999` and exact-one output masses are encoding diagnostics only. They are
   always reported, but they are not scientific boundary acceptance criteria.
4. Every mandatory proper-score, absolute finite-ensemble reliability,
   truth-referenced boundary, spatial/physical and operational family must pass
   without compensation.
5. No result produced by an earlier gate is promoted to primary success. The
   current locked-MC-dropout pair is retained unchanged as frozen development
   evidence and receives no retrospective threshold changes.

## Reconciliation rule

The compact primary result must carry both exact contract digests, controller
deployment/audit attestation, immutable provenance, every mandatory family
decision and the absolute randomized-rank and five-threshold high-SIC
diagnostics. Missing fields fail closed. Manuscript, claim ledger,
reproducibility and readiness must be updated atomically from that payload.
