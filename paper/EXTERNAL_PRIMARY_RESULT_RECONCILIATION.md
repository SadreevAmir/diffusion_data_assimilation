# External-primary compact-result reconciliation

Status: reconciled negative from the exact permitted recovery.

The four compact artifacts from
`external_2024_calendar_global_bias_confirm48_primary_retry1` were admitted as
one payload. Identity, seals, hashes, 48/48 completeness, 480 member records,
candidate-before-truth ordering and finiteness checks pass. The authoritative
gate reports `overall_eligible=false`: absolute rank reliability and
spatial/physical preservation fail. Proper-score, truth-relative boundary and
operational families pass. This closes the frozen primary without retuning,
resubmission or another external evaluation.

## Admissible evidence unit

Reconcile only the four compact artifacts produced by
`external_2024_calendar_global_bias_confirm48_primary_v1` from the sealed raw
stage `external_2024_calendar_global_bias_raw48_primary_v1`. Raw-stage
completion, queue or progress metadata, a development gate, or a different
confirmatory id is not a scientific substitute. Raw members and scoring truth
remain server-side.

The four artifacts must be admitted as one payload. Do not merge fields across
retries, summaries or controller messages. Before recording either a positive
or negative outcome, require all of the following:

- both exact amended-contract digests from
  `AMENDED_PRIMARY_EVALUATION_CONTRACT.md` and controller deployment/audit
  attestation for those identities;
- verified checkpoint, code, environment, dataset-manifest, runner/mode, seed
  and deterministic-comparator identities;
- successful input, raw-member and truth-bundle seal checks, every recorded
  hash verified, and explicit evidence that the complete candidate was formed
  before scoring truth was opened;
- exactly 48 attempted and completed cases, zero failed or non-finite cases,
  ten finite members per case, and exact candidate/raw/comparator case
  alignment;
- `no_compensation_across_families=true`, `gate.overall_eligible`, and an
  explicit decision for every mandatory proper-score, finite-ensemble
  reliability, boundary, spatial/physical and operational family;
- the absolute randomized-rank uniformity decision and truth-referenced
  high-SIC decisions at `q={0,.15,.90,.95,.99}`; `>=.999` and exact-one masses
  remain encoding diagnostics only;
- raw, candidate and deterministic-comparator decision-bearing metrics, paired
  date-level effects and every predeclared interval required by the signed
  contract, with units and regions stated.

Any missing artifact or field, non-finite value, identity or seal mismatch,
incomplete or misaligned case set, hash failure, premature truth access, or
disagreement between a family decision and its reported metrics fails
reconciliation. Never infer a Boolean decision from an aggregate metric and
never promote an earlier `overall_eligible` value.

## Atomic publication update

After the complete payload passes the checks above, update all five files in one
change:

1. `PAPER_DRAFT.md`: report the frozen candidate, comparator, decision-bearing
   effects and intervals, every mandatory-family decision and the
   no-compensation conclusion.
2. `CLAIM_LEDGER.md`: resolve C39 directly from the complete gate and retain the
   exact compact evidence and scope limitation.
3. `REPRODUCIBILITY.md`: record contract and provenance identities, all seal and
   hash decisions, candidate-before-truth ordering, case/member completeness and
   comparator alignment.
4. `RESEARCH_PLAN.md`: close the single primary evaluation without tuning,
   resubmission or substitution.
5. `PUBLICATION_READINESS.md`: reconcile the scientific status while retaining
   every authorship, venue, release or remaining minimum-tier blocker not
   resolved by this result.

Run `python3 paper/check_publication_artifacts.py` after the atomic update. A
passing local integrity audit checks internal consistency only; it never
overrides the signed gate.

## Decision branches

A positive primary admission requires `gate.overall_eligible=true`, every
mandatory family and operational integrity check passing, the absolute
randomized-rank criterion passing, and all five truth-referenced high-SIC
decisions passing. Only then may C39 and the manuscript record independent
generalization under this single frozen primary contract.

If any integrity requirement or mandatory family fails, record a negative
primary result, name each failed check and decision-bearing metric, retain the
failure in C39 and all five publication files, and do not resubmit, retune or
open another external evaluation.
