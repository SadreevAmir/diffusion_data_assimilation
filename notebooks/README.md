# Notebooks

Experiment and exploration notebooks live here so the repository root and
package directories stay focused on runnable code.

Launch Jupyter from the repository root when a notebook expects project modules
or relative data/checkpoint paths.
